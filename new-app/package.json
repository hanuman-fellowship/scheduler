{
  "name": "scheduler-app",
  "version": "1.0.0",
  "description": "Modern scheduler application - React frontend with Node.js backend",
  "private": true,
  "workspaces": [
    "shared",
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev:backend\" \"sleep 3 && npm run dev:frontend\"",
    "dev:backend": "npm run dev --workspace=backend",
    "dev:frontend": "npm run dev --workspace=frontend",
    "build": "npm run build --workspace=backend && npm run build --workspace=frontend",
    "test": "npm run test --workspace=backend && npm run test:run --workspace=frontend",
    "check": "npm --workspaces run check",
    "prisma": "npm run prisma --workspace=backend",
    "migrate": "npm run migrate --workspace=backend"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}